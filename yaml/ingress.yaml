apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress
  annotations:
    kubernetes.io/ingress.class: alb
    alb.ingress.kubernetes.io/scheme: internet-facing
    alb.ingress.kubernetes.io/target-type: ip
    # arn 값 수정 필요
    alb.ingress.kubernetes.io/certificate-arn: arn:aws:acm:ap-northeast-2:975049917890:certificate/a4d8adc1-3e2a-4238-9ed8-6a224a5dc41a
    alb.ingress.kubernetes.io/listen-ports: '[{"HTTP": 80}, {"HTTPS": 443}]'
    alb.ingress.kubernetes.io/ssl-policy: ELBSecurityPolicy-2016-08
spec:
  ingressClassName: alb
  rules:
    - host: enqn07.store
      http: 
        paths:
        - path: /
          pathType: Prefix
          backend:
            service:
              name: nginx-index-service
              port:
                number: 80
        - path: /menu.jsp
          pathType: Prefix
          backend:
            service:
              name: nginx-menu-service
              port:
                number: 80
        - path: /login.jsp
          pathType: Prefix
          backend:
            service:
              name: nginx-login-service
              port:
                number: 80
        - path: /join.jsp
          pathType: Prefix
          backend:
            service:
              name: nginx-join-service
              port:
                number: 80
        - path: /mypage.jsp
          pathType: Prefix
          backend:
            service:
              name: nginx-mypage-service
              port:
                number: 80
